on: 
  # issues:
  #   types: [opened]
  push:
    branches: 
      - main

jobs:
  action-comment:
    runs-on: ubuntu-latest
    steps:
      - name: github context
        run: echo '${{ toJSON(github.event) }}' | jq
        shell: bash
