on: 
  issues:
    types: [opened]

jobs:
  action-comment:
    runs-on: ubuntu-latest
    steps:
      - name: github context
        run: echo '${{ toJSON(github.event) }}' | jq
        shell: bash
